# Required: Your Anthropic API key
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Enable the Claude Agent SDK for deep analysis (default: false)
# Requires the claude CLI to be installed: npm install -g @anthropic-ai/claude-code
USE_AGENT_SDK=false
